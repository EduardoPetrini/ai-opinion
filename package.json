{
  "name": "ai-opinion",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "docker:build": "docker build -t ai-opinion .",
    "docker:run": "docker run -d -p 3000:3000 -p 3001:3001 --env-file .env --name ai-opinion ai-opinion",
    "docker:stop": "docker stop ai-opinion && docker rm ai-opinion"
  },
  "dependencies": {
    "nuxt": "^4.3.0",
    "uuid": "^13.0.0",
    "vue": "^3.5.27",
    "vue-router": "^4.6.4",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.14.0",
    "@types/node": "^25.2.0",
    "@types/uuid": "^11.0.0",
    "@types/ws": "^8.18.1"
  }
}

